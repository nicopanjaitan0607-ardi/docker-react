sudo: required
language: node_js
services:
  - docker

before_install:
  - docker build -t nicopanjaitan0607-ardi/docker-react -f Dockerfile.dev .

install: travis_wait 20 mvn install  

script:
  - docker run nicopanjaitan0607-ardi/docker-react npm run test -- --coverage